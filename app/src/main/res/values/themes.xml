<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for light mode -->
    <style name="Theme.EmotionDetection" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/text_primary</item>

        <!-- Surface colors -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>

        <!-- Error color -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>

        <!-- Custom colors for your app -->
        <item name="colorSurfaceVariant">@color/gray_light</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>

        <!-- Typography -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Card colors -->
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardForegroundColor">@color/text_primary</item>

        <!-- Button styles -->
        <item name="materialButtonStyle">@style/Widget.EmotionDetection.Button</item>
        <item name="materialButtonOutlinedStyle">@style/Widget.EmotionDetection.Button.Outlined</item>

        <!-- Chip colors -->
        <item name="chipStandaloneStyle">@style/Widget.EmotionDetection.Chip</item>

        <!-- Dialog theme -->
        <item name="android:dialogTheme">@style/ThemeOverlay.EmotionDetection.Dialog</item>
        <item name="android:alertDialogTheme">@style/ThemeOverlay.EmotionDetection.Dialog.Alert</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.EmotionDetection.Splash" parent="Theme.EmotionDetection">
        <item name="android:windowBackground">@color/primary_dark</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Button Styles -->
    <style name="Widget.EmotionDetection.Button" parent="Widget.Material3.Button">
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <style name="Widget.EmotionDetection.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Chip Style -->
    <style name="Widget.EmotionDetection.Chip" parent="Widget.Material3.Chip.Assist">
        <item name="chipBackgroundColor">@color/primary_light</item>
        <item name="chipStrokeColor">@color/primary</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="cornerRadius">16dp</item>
    </style>

    <!-- Dialog Theme Overlay -->
    <style name="ThemeOverlay.EmotionDetection.Dialog" parent="ThemeOverlay.Material3.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@color/surface</item>
    </style>

    <style name="ThemeOverlay.EmotionDetection.Dialog.Alert" parent="ThemeOverlay.Material3.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@color/surface</item>
    </style>

    <!-- Toolbar Theme -->
    <style name="ThemeOverlay.EmotionDetection.Toolbar" parent="ThemeOverlay.Material3.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white_alpha</item>
    </style>
</resources>